{
    auto d = py::dict();
    {% for name in items -%}
    d["{{ name }}"] = py::cast(&{{ namespace }}{{ name }}, py::return_value_policy::reference);
    {% endfor -%}
    enum_instances["{{ py_name }}"] = d;
}
