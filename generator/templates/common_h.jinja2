#pragma once
#include <pybind11/pybind11.h>
#include <BWAPI.h>
#include <BWAPI/Client.h>

namespace py = pybind11;

namespace PyBinding {

template <class Weakref, class BWSetContainer>
inline py::set set_converter(const BWSetContainer&amp; src){
    py::set out;
    for (auto it = src.cbegin(); it != src.cend(); ++it )
        out.add(py::cast(Weakref(*it)));
    return out;
}

template <class Weakref, class BWSetContainer>
inline py::set ptr_set_converter(const BWSetContainer&amp; src){
    py::set out;
    for (auto it = src.cbegin(); it != src.cend(); ++it )
        out.add(py::cast(Weakref(&amp;(*it))));
    return out;
}


{% for k in classes
 %}class {{ k }};
{% endfor %}

}
